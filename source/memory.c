/* Memory mapper and manager for the emulator */

#include <xc.h>                     // XC8 General Include File
#include <stdint.h>                 // For uint8_t definition

/**
 * Return the beginning address of the entire memory space
 * @return pointer to start address
 */
uint16_t getBaseAddress(void)
{
    //TODO
    return 0;
}

/**
 * Return the address of the byte after the end of the memory space
 * @return pointer to end address
 */
uint16_t getEndAddress(void)
{
    //TODO
    return 0;
}

/**
 * Return the beginning address of the read/write RAM space
 * @return pointer to RAM start address
 */
uint16_t getRamBaseAddress(void)
{
    //TODO
    return 0;
}